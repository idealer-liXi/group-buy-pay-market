<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.idealer01.infrastructure.dao.ISCSkuActivityDao">

    <resultMap id="dataMap" type="cn.idealer01.infrastructure.dao.po.SCSkuActivity">
        <id column="id" property="id"/>
        <result column="source" property="source"/>
        <result column="channel" property="channel"/>
        <result column="activity_id" property="activityId"/>
        <result column="goods_id" property="goodsId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>


    <select id="querySCSkuActivityBySCGoodsId" resultMap="dataMap">
        select source, channel, activity_id, goods_id
        from sc_sku_activity
        where goods_id = #{goodsId} and source = #{source} and channel = #{channel}
    </select>

    <select id="querySCSkuActivityListByActivityId" resultMap="dataMap">
        select source, channel, activity_id, goods_id
        from sc_sku_activity
        where activity_id = #{activityId}
    </select>

    <insert id="insertSCSkuActivity" parameterType="cn.idealer01.infrastructure.dao.po.SCSkuActivity">
        insert into sc_sku_activity(source, channel, activity_id, goods_id)
        values(#{source}, #{channel}, #{activityId}, #{goodsId})
    </insert>

    <update id="updateSCSkuActivity" parameterType="cn.idealer01.infrastructure.dao.po.SCSkuActivity">
        update sc_sku_activity
        set activity_id = #{activityId}, update_time = now()
        where source = #{source} and channel = #{channel} and goods_id = #{goodsId}
    </update>

    <update id="updateSCSkuActivityByActivityId">
        update sc_sku_activity
        set goods_id = #{goodsId}, source = #{source}, channel = #{channel}, update_time = now()
        where activity_id = #{activityId}
    </update>

    <delete id="deleteSCSkuActivityByActivityId" parameterType="java.lang.Long">
        delete from sc_sku_activity
        where activity_id = #{activityId}
    </delete>
</mapper>
