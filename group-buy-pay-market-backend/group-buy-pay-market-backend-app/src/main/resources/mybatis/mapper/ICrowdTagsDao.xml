<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.idealer01.infrastructure.dao.ICrowdTagsDao">
    <resultMap id="dataMap" type="cn.idealer01.infrastructure.dao.po.CrowdTags">
        <id column="id" property="id"/>
        <result column="tag_id" property="tagId"/>
        <result column="tag_name" property="tagName"/>
        <result column="tag_desc" property="tagDesc"/>
        <result column="statistics" property="statistics"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="queryCrowdTagsByTagId" resultMap="dataMap">
        select id, tag_id, tag_name, tag_desc, statistics, create_time, update_time
        from crowd_tags
        where tag_id = #{tagId}
    </select>

    <select id="queryCrowdTagsList" resultMap="dataMap">
        select id, tag_id, tag_name, tag_desc, statistics, create_time, update_time
        from crowd_tags
        order by id desc
    </select>

    <insert id="insertCrowdTags" parameterType="cn.idealer01.infrastructure.dao.po.CrowdTags">
        insert into crowd_tags(tag_id, tag_name, tag_desc, statistics, create_time, update_time)
        values(#{tagId}, #{tagName}, #{tagDesc}, 0, now(), now())
    </insert>

    <update id="updateCrowdTags" parameterType="cn.idealer01.infrastructure.dao.po.CrowdTags">
        update crowd_tags
        set tag_name = #{tagName}, tag_desc = #{tagDesc}, update_time = now()
        where tag_id = #{tagId}
    </update>

    <update id="updateCrowdTagsStatistics" parameterType="cn.idealer01.infrastructure.dao.po.CrowdTags">
        update crowd_tags
        set statistics = statistics + #{statistics}
        where tag_id = #{tagId}
    </update>

    <update id="updateCrowdTagsStatisticsTo">
        update crowd_tags
        set statistics = #{statistics}, update_time = now()
        where tag_id = #{tagId}
    </update>
</mapper>
